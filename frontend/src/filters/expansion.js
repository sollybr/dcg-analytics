// filters/expansion.js
//
// Adding a NEW filter never means editing this file (or any other filter
// file) -- it means adding a NEW file like this one and registering it
// in filters/index.js. That keeps parallel branches from touching the
// same lines.
export const expansionFilter = {
    key: 'excludeExpansions',
    paramName: 'exclude_exp',
};